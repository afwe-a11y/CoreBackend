<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tenghe.corebackend.iam.infrastructure.persistence.mapper.ApplicationTemplateMapper">
    <resultMap id="ApplicationTemplateMap" type="com.tenghe.corebackend.iam.infrastructure.persistence.po.ApplicationTemplatePo">
        <result column="template_code" property="templateCode"/>
        <result column="template_name" property="templateName"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO system_application_template
            (id, template_code, template_name, description, status, create_time, deleted)
        VALUES
            (#{id}, #{templateCode}, #{templateName}, #{description}, #{status}, #{createTime}, #{deleted})
    </insert>

    <update id="update">
        UPDATE system_application_template
        SET template_code = #{templateCode},
            template_name = #{templateName},
            description = #{description},
            status = #{status},
            deleted = #{deleted},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <select id="findById" resultMap="ApplicationTemplateMap">
        SELECT id,
               template_code,
               template_name,
               description,
               status,
               create_time,
               deleted
        FROM system_application_template
        WHERE id = #{id}
    </select>

    <select id="findByCode" resultMap="ApplicationTemplateMap">
        SELECT id,
               template_code,
               template_name,
               description,
               status,
               create_time,
               deleted
        FROM system_application_template
        WHERE template_code = #{code}
          AND deleted = 0
    </select>

    <select id="listAll" resultMap="ApplicationTemplateMap">
        SELECT id,
               template_code,
               template_name,
               description,
               status,
               create_time,
               deleted
        FROM system_application_template
        WHERE deleted = 0
    </select>

    <select id="findByIds" resultMap="ApplicationTemplateMap">
        SELECT id,
               template_code,
               template_name,
               description,
               status,
               create_time,
               deleted
        FROM system_application_template
        WHERE deleted = 0
          AND id IN
          <foreach collection="ids" item="id" open="(" close=")" separator=",">
              #{id}
          </foreach>
    </select>

    <update id="softDeleteById">
        UPDATE system_application_template
        SET deleted = 1,
            update_time = NOW()
        WHERE id = #{id}
    </update>
</mapper>
